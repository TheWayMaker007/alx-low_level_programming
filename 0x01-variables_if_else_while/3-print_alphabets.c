#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main - program that prints the alphabet in lowercase, and then in uppercase
 *Return: 0 (Successful)
 */
int main(void)
{
	int lowerC = 'a';
	int upperC = 'A';

	while (lowerC <= 'Z')
	{
	putchar(lower(lowerC);
	lowerC += 1;
	}

	while (upperC <= 'Z')
	{
	putcher(upperC);
	upperC += 1;
	}
	putchar('\n');
	return (0);
}
